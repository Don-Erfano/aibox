'use client';

import { NextPage } from 'next';

import { ConfigurationForm } from '@/components';

const Page: NextPage = () => <ConfigurationForm />;
export const dynamic = 'force-dynamic';

export default Page;
